#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <sstream>

using namespace std;

int main(){
    int n, m;
    cin >> n >> m;

    vector<int> obstaculosTecho(n, 0);
    vector<int> obstaculosPiso(n, 0);
}